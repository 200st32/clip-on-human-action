1. run "pip install -r requirements.txt" to install all the requirements
4. run "python inference.py --image_path[image_path] --type[model_type] --model_weights_path[model_weight]" to get inference result from single image
5. run "python class_accuracy.py --type[model_type] --model_weights_path[model_weight]" to get accuracy for every class
